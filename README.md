Install:

- Create database:

    ```shell
    create database with name intikom_test_be
    ```

- Copy & connect to database:

    ```shell
    cp .env.example .env
    ```

- Install & run project:

    ```shell
    npm install
    npm run dev
    ```